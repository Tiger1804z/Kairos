# Kairos Agent Instructions

Before starting any task, read:

- `docs/ai-memory/CURRENT_STATE.md`

Rules:

- `CURRENT_STATE.md` is the only AI memory file.
- Keep it short: one page maximum.
- It must describe current project state only, not history.
- Do not duplicate GitHub Issues, PRs, git log, or `KAIROS_DECISIONS.md`.
- If project state changes meaningfully, update `CURRENT_STATE.md`.
- If the file grows beyond one page, remove history and point to the source of truth instead.
